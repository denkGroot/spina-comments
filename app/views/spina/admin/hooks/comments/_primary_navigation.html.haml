%li{class: ('active' if controller_name.in? %w(comments))}
  = link_to spina.comments_admin_comments_path do
    = icon('comment')
    =t 'spina.comments.title'
    = icon('caret-right')

  %ul
    %li{class: ('active' if controller_name == 'comments'), data: {badge: Spina::Comments::Comment.unapproved.count}}
      = link_to Spina::Comments::Comment.model_name.human(count: 2), spina.comments_admin_comments_path

    %li
      = link_to '#', class: 'back-to-main-menu' do
        = icon('caret-left')
        =t 'spina.main_menu'